# Server port
PORT=3001

# Postgres connection (matches docker-compose.yml)
DATABASE_URL=postgresql://noah:noah@localhost:5434/noah

# Ollama endpoint (shared with frontend)
OLLAMA_URL=http://localhost:11434
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_DIM=768
